# Development Notes — 2026-07-24

## Starting State

The current project is `/home/smith/Agon/mystuff/AgonWolf3D`. Development
continues on the existing `dev` branch, which tracks `origin/dev` and was
neither ahead nor behind at the start of this session.

The tracked tree had no reported modifications. The existing untracked work
was preserved:

- `docs/`
- `src/asm/agnb.inc`

No source, generated asset, build artifact, or deployment was changed during
the initial inspection.

## AGNB Integration Audit

Reviewed the workflow guidance, eZ80 implementation notes, AGNB format
specification, loader API précis, image-load flow, and 2026-07-22 development
log in the requested order. Then inspected the Wolf3D build scripts, active
assembly include tree, image-generation pipeline, VDP routines, buffer-ID
conventions, application startup path, Git state, and the untracked
`src/asm/agnb.inc`.

The intended first integration scope is the RGBA2222 world-image families:
cubes, sprites, and distance walls. Fonts and UI images can remain on the
existing loose-file path initially. Sound effects must remain loose because
the AGNB 0.1 `AUDI` descriptor is deliberately unspecified.

This scope was confirmed before implementation. Sound effects are an intended
later AGNB use after the container's audio descriptor and finalization behavior
are specified. UI remains outside the first container because it currently
contains only a small number of files.

The font-loading follow-up confirmed that Wolf3D intentionally implements
proportional, coloured bitmap fonts at the application level. Each glyph is an
independent RGBA2222 bitmap with its own VDP buffer, dimensions, and vertical
offset; `font_bmp_plot` and `font_bmp_print` perform the proportional layout.
This is distinct from the VDP's native monochrome, monospaced, 256-glyph font
format. The existing glyph renderer will therefore be retained, and its
individual glyph images should eventually be packaged as ordinary AGNB `IMAG`
records. UI images should also move into AGNB eventually, but remain a lower
priority because there are comparatively few of them.

The principal integration findings are:

- `agnb.inc` is not yet included by the Wolf3D application.
- The loader depends on `FFSCALL`, `vdu_clear_buffer`, `vdu_load_buffer`, and
  `vdu_consolidate_buffer`, which are not present in the active Wolf3D
  dependency set.
- Its temporary traversal diagnostic depends on `DEBUG_WAITKEYPRESS`, which
  is also absent and should not be part of the production startup path.
- The existing Wolf3D routines already provide `umul24`, `vdu_buff_select`,
  `vdu_bmp_create`, `vdu_plot_bmp`, and `vdu_cls`.
- The loader's hard-coded 8 KiB scratch window at `$B7E000` conflicts directly
  with Wolf3D's runtime SRAM layout: `cell_status` begins at `$B7E000`,
  `cell_views` begins at `$B7E400`, and the sprite table begins at `$B7FC00`.
  Wolf3D must provide a different scratch pointer and capacity.
- Startup currently loads 308 cube images, 100 sprites, and 9 distance-wall
  images through generated loose-file routines after loading fonts and UI.
  The container call should replace only these three image-family passes in
  the first integration.
- Buffer IDs are assigned independently by several generators. The AGNB writer
  and assembly output must share one authoritative assignment and preserve
  every explicit ID unchanged.
- The current AGNB reader is the hardware-proven strict-layout implementation.
  Unknown-chunk seeking/skipping and general alignment helpers remain
  fail-closed stubs, so full extensible-reader conformance is a later step.
- The build deletes and recreates `tgt`; container generation must therefore
  occur after image generation and before assembly/deployment.

Created `docs/wolf3d-image-build-and-agnb-integration-precis.md` to document
the active path from source tiles and projected geometry through transformed
PNGs, RGBA2222 payloads, generated assembly, runtime loading, and deployment.
It identifies stage 91 as the central AGNB build seam: one shared catalog
should drive both the preserved assembly buffer constants/lookup tables and
the new `tgt/images.agnb`, preventing buffer-ID or metadata drift.

Added the environment-level `scripts/setup_emulator.py`, deliberately outside
the application build path. It creates an isolated `.emulator` profile using
symlinks to the shared Fab Agon emulator executable, firmware, and standard
SD-card system files; copies the project's current `tgt` tree to
`.emulator/sdcard/mystuff/AgonWolf3D/tgt`; and writes the project-local
`sdcard/autoexec.txt`. The autoexec was verified byte-for-byte to contain only
the following three CRLF-terminated lines:

```text
SET KEYBOARD 1
cd mystuff/AgonWolf3D/tgt
load wolf3d.bin
```

The setup script was run successfully. The local target file count matched the
source target, and the copied `wolf3d.bin` matched byte-for-byte.

Launched the project-local emulator from `.emulator` with SDL explicitly using
the Wayland video driver. The verified three-line autoexec loaded
`mystuff/AgonWolf3D/tgt/wolf3d.bin`, and the user confirmed that the application
works. This establishes the current `dev` branch and target as the known-good
functional baseline before AGNB integration changes begin.

Restored the standalone `agon-utils` environment at
`/home/smith/Agon/mystuff/agon-utils` from the published
`agonvideo/modern-python-build` branch. The previous incomplete directory,
which contained only generated example output and no Git checkout, was
preserved as `/home/smith/Agon/mystuff/agon-utils-pre-setup-20260724`.

On 2026-07-25, that safety backup was audited before removal. Its only 21
files—the slideshow `app.bin` and 20 generated RGBA2 images—matched
`/home/smith/Agon/mystuff/agon-utils/examples/slideshow/tgt` byte-for-byte.
The incomplete backup contained no Git repository, source, or unique work and
was deleted.

Created the standalone checkout's `.venv` with Python 3.14.6, confirmed against
the current stable Python release, installed `agonutils` 1.1.0 in editable
mode, and installed Pillow 12.3.0. The prescribed
`.venv/bin/python tests/test_agonutils.py` check passed. `pip check` reported no
broken requirements; the expected image and SIMZ APIs were present; a
4,096-byte SIMZ round trip passed; and the extension linked successfully
against FFmpeg and libpng.

The standalone setup did not initially install `agonutils` into AgonWolf3D's
separate `.venv`. Corrected that by installing the local checkout in editable
mode into `/home/smith/Agon/mystuff/AgonWolf3D/.venv` with build isolation and
dependency downloads disabled. The consumer environment now imports
`agonutils` 1.1.0 from
`/home/smith/Agon/mystuff/agon-utils/agonutils.cpython-314-x86_64-linux-gnu.so`;
the agonutils test passes under the Wolf3D interpreter and `pip check` reports
no broken requirements. Added the cross-project consumer setup and verification
commands to both projects' workflow handoff notebooks.

## Sequential Implementation Checklist

- [x] 1. Define and confirm the first integration scope: cube, sprite, and
  distance-wall RGBA2222 images only, leaving fonts, UI, maps, and sound
  effects unchanged.
- [x] 2. Commit and push the initial AGNB documentation and unintegrated
  hardware-proven loader on `dev`, establishing a clean departure point before
  code changes.
- [x] 3. Audit the active bitmap-font path and confirm that Wolf3D
  intentionally uses proportional, coloured, one-bitmap-per-glyph rendering;
  defer glyph and UI containerization until after the initial world-image
  integration.
- [x] 4. Trace and document the current image-generation pipeline and its AGNB
  hooks in `docs/wolf3d-image-build-and-agnb-integration-precis.md`.
- [x] 5. Add and run the project-local emulator setup, copy the current target
  into its isolated SD card, launch with SDL Wayland, and confirm the existing
  application works as the known-good functional baseline.
- [x] 6. Restore and verify the standalone `agon-utils` checkout and its
  project-local Python 3.14.6 environment, including the native extension,
  Pillow dependency, API/linkage checks, and SIMZ round trip.
- [x] 7. Confirm that `src/asm/wolf3d.asm` is the active application entry
  point used by the final assembly stage, and remove the obsolete
  `src/asm/wolf3d1.asm` and the seven unused modernization-era assembly
  sources. Validate the cleanup with a complete build, successful final
  assembly, and working game run.
- [x] 8. Trace startup from `start` through the complete image-loading phase,
  including ordered subroutine calls, source-file ownership, buffer IDs,
  global state, scratch memory, VDP helpers, and the loading-screen
  animation/debug path; record it in
  `docs/current-image-loading-startup-trace.md`.
- [x] 9. Include the hardware-proven AGNB loader in the active application
  and use assembler-only runs to discover its actual integration
  dependencies. Correct the include to reference `agnb.inc`.
- [x] 10. Replace Wolf3D's older `mos_api.asm` with the container harness's
  `mos_api.inc`, bringing in the FatFS definitions and `FFSCALL` wrapper
  required by the AGNB reader.
- [x] 11. Remove the harness-only `DEBUG_WAITKEYPRESS` call from the imported
  loader without importing its debug/function dependency tree.
- [x] 12. Replace Wolf3D's existing `maths.asm` with the container harness's
  superset `maths.inc`, supplying the required `umul24` implementation while
  retaining Wolf3D's existing BCD and random-number labels.
- [x] 13. Import `macros.inc` and include it immediately after `mos_api.asm`,
  supplying `SIGN_UHL` and the shared macros required by the expanded math
  module.
- [x] 14. Transplant only `vdu_load_buffer`, `vdu_clear_buffer`, and
  `vdu_consolidate_buffer` from the loose AGNB VDU source into Wolf3D's
  existing `vdu.asm`, avoiding duplicate definitions from importing the
  complete VDU file.
- [x] 15. Assemble the final application successfully with `agnb.inc` and its
  production dependencies present; regenerate `tgt/wolf3d.bin`.
- [x] 16. Deploy the newly assembled target to the isolated project-local
  emulator profile and launch Fab Agon with SDL Wayland.
- [x] 17. Complete the emulator runtime smoke test: allow startup loading to
  finish, enter the application, and confirm that the dependency imports did
  not regress the known-good application.
- [x] 18. Establish one build-time image catalog/manifest from the existing
  database outputs, preserving current `BUF_*` values, dimensions, paths, and
  deterministic ordering.
- [x] 19. Add a Wolf3D AGNB writer and an independent structural test, and
  schedule container generation after image conversion but before assembly
  and deployment.
- [x] 20. Remove the `$B7E000` conflict by giving `agnb.inc` a loader-owned
  8 KiB buffer in the application image.
- [x] 21. Remove or deliberately integrate the loader's remaining per-record
  plotting diagnostics before connecting it to production startup.
- [x] 22. Add an explicit startup error path and replace only the cube/panel
  loose-load loop with `agnb_load_images`; retain the sprite and distance-wall
  loose-load loops.
- [x] 23. Preserve the generated buffer constants and rendering lookup tables
  while stopping generation of obsolete filename strings and per-image load
  routines for containerized assets.
- [ ] 24. Verify container bytes and IDs, assemble, deploy to the emulator,
  test malformed and truncated input, then validate the complete catalog and
  loading time on physical hardware.
- [ ] 25. After the strict path is stable, implement unknown-chunk and
  unsupported-form skipping for full AGNB 0.1 reader conformance.

## Follow-up TODOs

- [x] Audit the active bitmap-font build and startup path. Preserve Wolf3D's
  intentional proportional, coloured, one-bitmap-per-glyph renderer and plan
  to package those glyph bitmaps as ordinary AGNB `IMAG` records in a later
  phase.
- [ ] Migrate the comparatively small UI image set to AGNB after the
  higher-priority world-image and glyph-image migrations are stable.
- [ ] Specify the AGNB audio descriptor and VDP sample-finalization behavior,
  then evaluate migrating sound effects into the container.
- [ ] Reproduce the post-exit Guru Meditation from a clean hardware boot,
  without launching the music player first. Determine whether it is a Wolf3D
  shutdown regression or pre-existing VDP state left by the music player.
### Current image-loading trace

- Added `docs/current-image-loading-startup-trace.md`.
- Traced the active `src/asm/wolf3d.asm` startup path in execution order through font, UI, panel/cube, sprite, and distance-wall image loading.
- Recorded each relevant subroutine's source file, generated tables and loaders, VDP helper chain, progress/debug drawing order, buffer-ID ranges, self-modifying code, global state, and scratch/fixed-memory ownership.
- Captured the integration consequence that the loose-file `mos_load` calls live inside the generated per-image routines, before `vdu_load_img`, so the first AGNB implementation cannot be a drop-in replacement at `vdu_load_img` alone.

### Removed abandoned modernization sources

Compared the assembly sources in `src/asm` with the direct include set in the
active `src/asm/wolf3d.asm`. Removed the following tracked files because they
were unused remnants of an earlier, unfinished modernization effort and were
creating ambiguity about the active implementation:

- `src/asm/game_state.asm`
- `src/asm/images_CMP.asm`
- `src/asm/img_load_dif_buf.asm`
- `src/asm/maps1.asm`
- `src/asm/outro.asm`
- `src/asm/panels.asm`
- `src/asm/player1.asm`

Committed only these seven deletions in a dedicated cleanup commit. The active
application and its include tree were not changed by that commit.

After the deletion commit, enabled every manual override flag beneath the
“I find it easier to simply comment out the scripts I don't want to run”
control block in `build/scripts/build_00_all_the_things.py`. A complete build
and final application assembly succeeded, and the resulting game remained
functional. This confirms that none of the seven deleted files was an implicit
build-time dependency.

### First Wolf3D AGNB container

- Added `build/scripts/build_05a_make_panels_agnb.py`, adapting the
  hardware-proven RIFF/AGNB writer from
  `agon-utils/examples/agnb/container/scripts/do_assembly.py`.
- The writer consumes the same deterministic `cube` query used by
  `build_91_asm_images.py`, assigns the existing explicit buffer IDs from
  `0x0100` through `0x0233`, validates every dimension and RGBA2222 byte count,
  and writes `tgt/images.agnb`.
- Added the stage to `build_00_all_the_things.py` immediately after stage 05
  image conversion. This recreates the container after a full build deletes
  `tgt` and before final assembly/deployment.
- Generated a 1,567,568-byte container containing all 308 panels.
- Independently parsed the result with
  `agon-utils/examples/agnb/container/scripts/view_agnb.py`. Confirmed the
  complete RIFF structure, record count, ID range, dimensions, DATA sizes, and
  byte-for-byte equality of every DATA payload with its catalogued `.rgba2`
  source.
- Added durable writer and validator reuse pointers to both the Wolf3D and
  agon-utils AGNB `docs/codex-workflow-notebook.md` handoff documents.

### Panels-only assembly integration

- Regenerated `src/asm/images.asm` without the cube load jump table, 308
  individual loose-file cube loaders, or their filename strings. The cube
  `BUF_*` constants, `cube_num_panels`, and `cube_buffer_id_lut` remain
  unchanged; sprite and distance-wall loose-load code remains present.
- Replaced only the cube/panel block in `init` with `agnb_load_images`.
  Fonts, UI, sprites, distance walls, and sound effects retain their prior
  loading paths.
- Replaced the AGNB harness's per-record diagnostic with
  `img_load_agnb_progress`, called immediately after a record has been fully
  streamed, consolidated, and created as a bitmap. The callback redraws the
  existing splash, plots the new panel for debugging, moves BJ, prints an
  ordered panel number and elapsed time, and flips the display.
- Added a startup failure path that displays the AGNB error code, waits for a
  key, and returns to MOS without entering the game.
- Removed the loader's `$B7E000` conflict by allocating an 8 KiB
  loader-owned `agnb_io_buffer` in the application image. The existing
  tail-of-program `filedata` scratch remains dedicated to subsequent loose
  sprite and distance-wall loads.
- Final assembly succeeded. The resulting 44,604-byte `wolf3d.bin` and
  1,567,568-byte `images.agnb` were deployed byte-for-byte to the isolated
  emulator profile, and the emulator was launched with SDL Wayland for runtime
  validation.

### Complete container-based panel build

- Renamed `build_91_asm_img_load.py` to `build_91_asm_images.py` and renamed
  its master flag to `do_91_asm_images`. The stage is now described accurately
  as generating image buffer metadata plus the remaining sprite and
  distance-wall loose loaders; it no longer presents itself as the panel
  deployment path.
- Retained stage 05 RGBA2222 generation as the required intermediate consumed
  by stage 05a. The deployed panel asset is `tgt/images.agnb`.
- Enabled every manual override flag and ran
  `build/scripts/build_00_all_the_things.py` from target deletion through final
  assembly. Every stage completed, `images.agnb` was recreated with 308 panel
  records, and `wolf3d.asm` completed both assembler passes.
- Independently parsed the rebuilt container: IDs remain `0x0100`–`0x0233`,
  size remains 1,567,568 bytes, and SHA-256 remains
  `20d38c3477e6e320dd5783719c25aa51f0efb6f4b69692fc90e4332b1c791659`.
- Confirmed `tgt/panels` still contains 408 RGBA2222 intermediates, while only
  the 308 cube/panel records are containerized. The generated assembly retains
  `sprite_load_panels_table` and `dws_load_panels_table` and contains no cube
  loose-load table.
- Deployed the complete target tree to the isolated emulator profile, verified
  the deployed AGNB and binary byte-for-byte, and launched the emulator with
  SDL Wayland.
- Corrected the deployment layout: stage 05 still creates all 408 panel/sprite
  RGBA2222 intermediates, but stage 05a now removes the 308 catalogued cube
  files only after `images.agnb` has been written successfully. `tgt/panels`
  and the emulator copy now contain exactly the 100 loose sprite files, while
  the container retains all 308 cube/panel records. There is no cube/sprite
  filename overlap.

## Semi-major milestone: hardware AGNB panel loading

The first production-facing AGNB integration is working on physical Agon
hardware:

- all 308 cube/panel bitmaps load from the single `images.agnb` container;
- panel loading is substantially faster than the former loose-file process;
- the splash/progress/debug update remains active between panel records;
- the 100 sprites and nine distance walls continue through their existing
  loose-file paths;
- the game enters normal play and remains functional; and
- the deployed `tgt/panels` directory contains only the 100 sprite payloads.

One post-exit Guru Meditation occurred during this hardware test. Its cause is
not established. The music player had been launched at boot before Wolf3D, and
it is already known occasionally to leave the VDP in a problematic state after
extensive buffer activity. Treat this as an open reproduction task rather than
attributing it to AGNB. The next exit test should begin from a clean boot
without launching the music player.

This is a semi-major milestone: the container writer, full build integration,
streaming assembly loader, per-record progress hook, deployment layout, and
physical-hardware gameplay path have all been exercised successfully.

## Post-milestone code-review follow-ups

Reviewed milestone commit `5c9cc95` against its clean departure point
`c40ffea`. The successful hardware path is internally coherent: the container
has 308 records at IDs `0x0100`–`0x0233`, those IDs and dimensions match the
database and generated assembly, `tgt/panels` contains the intended 100 loose
sprite files, and there is currently no cube/sprite filename overlap.

The review identified the following durable follow-up work:

1. [x] Generate containerized cube/panel RGBA2222 intermediates under the
   build tree, retain only loose sprite payloads in `tgt/panels`, and make the
   AGNB stage consume the build-tree inputs without deleting them.
2. [x] Establish a single shared source for the panel buffer-ID base and catalog
   assignments, then assert that AGNB record IDs match the generated assembly
   constants so the two outputs cannot silently drift.
3. [x] Route AGNB initialization failures through VDP/application cleanup before
   returning to MOS; the current NZ startup exit bypasses buffer clearing,
   channel shutdown, screen-mode restoration, and cursor restoration.
4. [x] Retain the current simple hardware SD deployment behavior. A staged,
   recoverable replacement was reviewed and deliberately declined as not worth
   the added complexity for this workflow.

### Follow-up 1: separate build-only panel intermediates

- Stage 05 now derives disjoint cube and sprite filename sets from the database.
  It writes the 308 containerized cube/panel RGBA2222 files to
  `build/panels/rgba2` and the 100 still-loose sprite files to `tgt/panels`.
- The conversion fails on cube/sprite filename overlap, uncatalogued
  transformed PNGs, or incomplete catalog output.
- Stage 05a now consumes `build/panels/rgba2` and no longer deletes its inputs.
  The RGBA2222 review utility follows the same split.
- Added `/build/panels/rgba2/` to `.gitignore`; these 308 reproducible
  container inputs remain available locally without becoming tracked build
  artifacts. Deleted and regenerated the directory to verify the rule and
  stage-05 recreation path.
- A complete master build and final two-pass assembly succeeded. The rebuilt
  container is byte-for-byte unchanged at 1,567,568 bytes with SHA-256
  `20d38c3477e6e320dd5783719c25aa51f0efb6f4b69692fc90e4332b1c791659`;
  all 308 DATA payloads match the retained build-tree inputs. The assembled
  `wolf3d.bin` is 44,604 bytes.
- Refreshed the isolated emulator profile and verified all 275 deployed target
  files byte-for-byte plus the exact three-line CRLF autoexec. Launched Fab
  Agon with the SDL Wayland video driver for runtime testing.

### Follow-up 2: shared image catalog and ID contract

- Added `build/scripts/image_catalog.py` as the single owner of the starting
  buffer ID, ordered cube/sprite/distance-wall database queries, continuous ID
  assignment, dimensions, and payload paths.
- Stage 05 uses its catalog queries to split outputs; stage 05a uses its cube
  entries directly for AGNB metadata and IDs; and stage 91 uses the same
  catalog entries to generate every `BUF_*` constant and lookup table.
- Stage 91 now parses `tgt/images.agnb` and fails the build unless all ordered
  cube `BHDR` IDs exactly match the shared catalog. The catalog also rejects
  duplicate names, case-colliding assembly symbols, invalid dimensions, and
  reserved or overflowing buffer IDs.
- Regenerating `src/asm/images.asm` before the full build produced the exact
  previous SHA-256
  `aad0a8aa65f2e7eda087dc1b6385f554a5b935ecdc4ad32b25bcc5ab8e3cb9ca`.
  The complete master build, 308-record ID assertion, and final two-pass
  assembly all succeeded. Existing ranges remain cubes `0x0100`–`0x0233`,
  sprites `0x0234`–`0x0297`, and distance walls `0x0298`–`0x02A0`.

### Follow-up 3: shared failed-startup cleanup

- Extracted the existing normal-exit VDP shutdown sequence under the
  `app_cleanup` label. `main_end` retains its existing fall-through behavior.
- `start` now checks the `init` result, calls `app_cleanup` on NZ, and returns
  through the normal register-restoring `exit` path without calling `main`.
- Both exit paths now clear all VDP buffers, disable the additional audio
  channels, restore screen mode zero, and turn the cursor on.
- Final two-pass assembly succeeded. The resulting 44,612-byte
  `tgt/wolf3d.bin` has SHA-256
  `7efd3ad4483a9c1ba88389ac980ba70536d6df1ed08bb51901a773686acae41b`.
- Corrupted only the emulator copy of `images.agnb` by changing its leading
  `RIFF` signature to `XIFF`. The application displayed error `04`, waited for
  input, and returned cleanly to MOS; the user confirmed the failure-path test
  passed. The canonical `tgt/images.agnb` remained unchanged.
- Added the established stack-return `printInline` routine to `functions.asm`.
  Converted the single-site AGNB failure and loading-panel messages to inline
  `ASCIZ` strings while retaining shared named strings such as `loading_time`.
- Added `Press any key to return to MOS` on the line after the AGNB error code,
  followed by the existing display flip and `waitKeypress`. Final two-pass
  assembly succeeded.
- Applied the inline-string convention across the remaining applicable startup
  messages. `Loading UI`, `Loading time:`, and the emulator/hardware status
  messages now use `printInline` at their issuance sites. Renamed the shared
  proportional-font string `hello_world` to `welcome_message`; it remains
  global because three callers pass its address to `font_bmp_print`, which is
  a different renderer from `printInline`. Renamed the intentionally shared
  loading-complete prompt to `press_any_key`. Updated all assembly dependencies
  and completed final two-pass assembly successfully.

## Minor checkpoint: reviewed AGNB integration hardening

The user completed a code review and confirmed the restored-container game
works in the emulator. This checkpoint includes the build-tree panel
intermediates, shared image catalog and ID assertion, clean failed-startup
shutdown, verified corrupt-header error path, inline-message cleanup, generated
database state, and the reviewed working application binary.

## Sprite AGNB integration

- Extended stage 05 so all 308 cube and 100 sprite RGBA2222 intermediates are
  retained together in the ignored `build/panels/rgba2` directory. The stage
  removes the obsolete loose `tgt/panels` deployment directory.
- Renamed the container writer to
  `build/scripts/build_05a_make_images_agnb.py` and expanded it to emit all 408
  ordered cube-and-sprite records from the shared catalog.
- Extended the stage-91 catalog assertion across both families. Generated
  assembly retains all cube/sprite constants, counts, and renderer lookup
  tables, but emits neither family's jump table, individual MOS loader, nor
  filename strings. Distance walls remain loose.
- Removed the separate sprite `img_load_main` startup pass. One
  `agnb_load_images` call now loads both families, and its splash text says
  `Loading image` rather than `Loading panel`.
- The focused generator chain and complete master build both succeeded. The
  final container contains 408 byte-verified records: cubes at
  `0x0100`–`0x0233` and sprites at `0x0234`–`0x0297`. It is 1,739,624 bytes
  with SHA-256
  `7828fcd4a97344181fe5d68d3017c39b32d77a0b7874b509dafdbddbdaa6845c`.
  Final two-pass assembly produced a 38,128-byte `wolf3d.bin` with SHA-256
  `7c88019f491082b4169704929d3e3e4f4ba1b90bb372634facd526d16ba64344`.

Sprite migration verification:

1. [x] Run the complete master build and final two-pass assembly.
2. [x] Independently parse all 408 records and compare every ID, dimension,
   and DATA payload byte-for-byte with the shared catalog and build input.
3. [x] Refresh the isolated emulator target and verify it matches `tgt`
   exactly, contains nine loose distance-wall files, and has no `tgt/panels`.
4. [x] Launch the refreshed build in Fab Agon under SDL Wayland.
5. [x] Confirm startup, sprite rendering, and gameplay in the running
   emulator.
6. [ ] Deploy the same `tgt` tree to the physical SD card. The expected
   `/media/smith/AGON` mount was absent when deployment was attempted.
7. [ ] Confirm startup, sprite rendering, gameplay, and clean exit on physical
   hardware.

### Empty-sprite AGNB failure and resolution

- The first combined-container runtime test stopped after 327 completed images
  with AGNB error `07` while entering sprite `23_047` (`BUF_23_047`,
  `0x0247`). This is a valid 1-by-1 transparent placeholder with a one-byte
  zero DATA payload.
- Independent parsing proved its `LIST`, `BUFR`, nested chunks, sizes, and
  alignment bytes correct. Temporary runtime diagnostics then proved both the
  tracked and FatFS offsets were `0x185C84` after reading the next LIST header,
  whose bytes were correctly `4C495354`.
- Instruction-level stage markers isolated the failure to the LIST
  minimum-size subtraction. `wolf3d.lst` revealed that `ez80asm` had assembled
  `agnb_list_type_size+3*agnb_chunk_header_size` as left-to-right
  `(4+3)*8 = 56` (`0x38`) rather than `4+(3*8) = 28` (`0x1C`).
- Reordered the source expression to
  `3*agnb_chunk_header_size+agnb_list_type_size`; the listing now emits
  `0x1C`. Removed all temporary diagnostics, rebuilt, and relaunched.
- The user confirmed the corrected build is a good application. The small
  valid placeholder exposed a pre-existing reader bug; neither the generated
  AGNB container nor the placeholder asset was malformed.
- Propagated the expression fix and failure-mode documentation to the
  canonical `agon-utils/examples/agnb` project. Its test harness assembled
  successfully, and its generated listing now encodes the intended `0x1C`
  minimum. A new master dev-log entry records the investigation. Its Python
  writer tests remain unavailable until that checkout's omitted shared
  processed assets are restored.

## Distance-wall AGNB integration

1. [x] Redirect all nine DWS RGBA2222 outputs from `tgt/dws` to the ignored
   build-only directory `build/dws/rgba2`.
2. [x] Extend the shared catalog, AGNB writer, and stage-91 ID assertion across
   cube, sprite, and DWS families.
3. [x] Preserve the nine DWS constants, count, and renderer LUT while removing
   their generated jump table, MOS loaders, and filename strings.
4. [x] Remove the final world-image `img_load_main` startup pass. One
   `agnb_load_images` call now creates all world-image buffers.
5. [x] Run the focused generator chain and final assembly.
6. [x] Run the complete master build from its destructive `tgt` recreation
   through final two-pass assembly.
7. [x] Independently parse and byte-compare all 417 records: 308 cubes at
   `0x0100`–`0x0233`, 100 sprites at `0x0234`–`0x0297`, and nine distance
   walls at `0x0298`–`0x02A0`.
8. [x] Confirm the deployable tree contains neither `tgt/panels` nor
   `tgt/dws`, and generated `images.asm` contains no world-image loose-loader
   blocks.
9. [x] Confirm all distance-wall records load, followed by successful startup,
   gameplay, and clean exit in the emulator. Distance walls are intentionally
   not rendered in the current game, so this verifies loading rather than
   visible rendering.
10. [x] Deploy and verify the same build on physical hardware.

The reviewed target has now been deployed to the mounted hardware SD card and
verified byte-for-byte against local `tgt`; the user subsequently confirmed
the on-device runtime test. The deployment helper now replaces only
`mystuff/AgonWolf3D/tgt` rather than clearing the entire application directory,
matching the established hardware-copy workflow. Its unreliable preflight
read-only flag check was removed; the Python copy completed successfully.

## Semi-major milestone: all world images containerized

The user confirmed the reviewed 417-image build loads and runs correctly on
physical Agon hardware. Panels/cubes, sprites, and distance walls now load from
one `images.agnb`; no loose world-image files or generated per-image MOS
loaders remain. The same artifacts passed a complete build, two-pass assembly,
independent byte-level container validation, emulator testing, SD-card
deployment verification, and physical gameplay testing.

The application still produces a Guru Meditation on program exit on hardware.
This remains an unresolved exit/VDP-cleanup defect and is not considered a
failure of world-image loading: startup, all 417 buffer loads, and gameplay
complete successfully before the exit fault.

Code-review cleanup removed the one-time `legacy_sprite_rgba_dir` and
`legacy_dws_rgba_dir` parameters and deletion branches from their standalone
stages and the master build. The normal full build already recreates `tgt`,
and no active stage writes either obsolete directory. A subsequent complete
master build and final assembly passed, reproduced all three verified hashes
below exactly, and left both loose world-image directories absent.

Verified artifacts:

- `tgt/images.agnb`: 1,789,336 bytes, SHA-256
  `5ebba2805a2b3a038097ba12c047f9265cb58b1346e0ce7efb03be2c1cd7c0a3`;
- `tgt/wolf3d.bin`: 37,559 bytes, SHA-256
  `9ac18ecd2813353c05d5c701fcc75e1115dddfe4309945c289f834b1c4e0260a`;
- `src/asm/images.asm`: 16,191 bytes, SHA-256
  `9d1812dd69f9353ee57d8d9f5fbe3a4e299a4a03ed503853cb254e3cf8aacc9d`.

## Retired unused Retro Computer font

- Confirmed only ITC Honda is selected by any `font_bmp_print` caller. Retro
  Computer was generated, deployed, included, and loaded at startup but never
  used.
- Removed the Retro Computer include and startup call, its generated assembly,
  source-art directory and XCF file, generated metadata, and 39 deployed glyph
  payloads.
- Stage 91a now generates only ITC Honda and removes obsolete rows for other
  fonts from `tbl_91a_font`. ITC Honda retains its established buffer range
  beginning at `0x1100`.
- The complete master build and final two-pass assembly succeeded without any
  active Retro Computer references or regenerated Retro Computer artifacts.
- The refreshed emulator build completed startup and gameplay successfully
  with ITC Honda as the sole retained custom bitmap font.

## UI AGNB bootstrap integration

1. [x] Preserve the reviewed Retro Computer removal as minor checkpoint
   commit `7c6ba8a`.
2. [x] Redirect the 11 core and 20 BJ UI RGBA2222 files to ignored
   `build/ui/rgba2` intermediates.
3. [x] Generate `tgt/ui.agnb` with the established buffer-ID ranges
   `0x2000`–`0x200A` and `0x2100`–`0x2113`.
4. [x] Remove generated loose UI loaders and filenames while retaining buffer
   constants and counts.
5. [x] Generalize the AGNB loader's filename and completed-image callback.
6. [x] Load UI before changing screen mode, printing one `.` per completed
   bitmap without selecting or plotting it.
7. [x] Retain ITC Honda and the animated splash callback for all 417 world
   images after entering the game's double-buffered mode.
8. [x] Run the complete master build and final two-pass assembly.
9. [x] Independently validate all 31 UI IDs, dimensions, alignment padding,
   and DATA payloads byte-for-byte.
10. [x] Confirm the clean target contains `ui.agnb` and no loose `tgt/ui`.
11. [x] Launch in the emulator and confirm breadcrumbs, animated world-image
    loading, startup, and gameplay.

The build produced a 181,928-byte `tgt/ui.agnb` with SHA-256
`3a9ed09a8d4a9ed973f78f498fa39dc2b87c40fb595002f2ca2f61e971a643b0`
and a 32,842-byte `tgt/wolf3d.bin` with SHA-256
`779ffdeb6cdc2c83614a1ce75f852dae0c041c903b43e4d136ca3fa8ec3b6f24`.

### Consecutive-container reader-state defect

The first runtime test completed `ui.agnb`, advanced through the panel records
in `images.agnb`, then failed during the sprite records with AGNB error `08`
(bounds). The reader's logical `agnb_state_file_offset` had previously relied
on zero-initialized application memory and was not reset by `agnb_open`.
Consequently, the second traversal began logically 181,928 bytes into the
world container even though FatFS had physically opened it at byte zero. The
parser therefore reached the declared RIFF end early.

`agnb_open` now explicitly resets the four-byte logical file offset and last
read count for every new container. Assembly passed, the emulator target was
refreshed byte-for-byte, and the corrected application was relaunched for
runtime confirmation.

The user confirmed the corrected application completed all image loads and
started successfully in the emulator. The canonical AGNB project received the
same per-open reset, a double-traversal runtime regression, automated
lifecycle checks, and documentation of the failure signature and resolution.

## ITC Honda AGNB integration

1. [x] Preserve the 64 real glyph records, including the generated space
   glyph, at their established character-derived IDs.
2. [x] Move the RGBA2222 glyph payloads and generated definition CSV from
   `tgt/fonts` to ignored `build/fonts` intermediates.
3. [x] Generate `tgt/font.agnb` while retaining proportional width, height,
   `y_offset`, fallback mappings, and buffer constants in assembly.
4. [x] Remove all generated per-glyph MOS loaders and pathname strings.
5. [x] Load `font.agnb` through the shared breadcrumb path between `ui.agnb`
   and `images.agnb`.
6. [x] Complete a clean full build and two-pass assembly.
7. [x] Independently validate all font and UI container IDs, dimensions,
   alignment, and payload bytes.
8. [x] Confirm the target contains no loose `fonts` or `ui` directories.
9. [ ] Verify all three consecutive containers, proportional font rendering,
   startup, and gameplay in the emulator.

Verified clean-build artifacts:

- `font.agnb`: 12,700 bytes, SHA-256
  `76b2965ae306d8199b20f1f65e5eb9d22a9b7117c30336c87b47541b1a1287bc`;
- `ui.agnb`: 181,928 bytes, SHA-256
  `3a9ed09a8d4a9ed973f78f498fa39dc2b87c40fb595002f2ca2f61e971a643b0`;
- `images.agnb`: 1,789,336 bytes, SHA-256
  `5ebba2805a2b3a038097ba12c047f9265cb58b1346e0ce7efb03be2c1cd7c0a3`;
- `wolf3d.bin`: 29,132 bytes, SHA-256
  `f2bdd3961cebe4a00c86327ed5d52a8e52f4efe85560eca8e3c8197106274657`.

### Generic callback API code-review cleanup

- Removed all Wolf3D filenames, wrapper entry points, callback modes, and
  application callback references from `agnb.inc`.
- `agnb_load_images_with_callback` accepts `DE=filename` and
  `HL=completed-image callback`; `agnb_load_images` remains compatible by
  selecting an internal no-op callback.
- `agnb_call_image_callback` uses the application callback pointer and
  `jp (hl)`, allowing the callback's normal `ret` to resume the loader.
- `wolf3d.asm` now owns all three filenames and explicitly pairs the UI and
  font containers with `img_load_agnb_breadcrumb`, and the world container
  with `img_load_agnb_progress`.
- `img_load.asm` remains the owner of both Wolf3D-specific callback
  implementations.
- Final two-pass assembly succeeded. The refactored `wolf3d.bin` is 29,120
  bytes with SHA-256
  `49ab3f46c97e7e94f230a9374569b740b2c2e15701c33e8ba4ebbd5cbe23e6d1`.
- The emulator runtime test passed. Documented the existing `CALL_HL`,
  `CALL_IX`, and `CALL_IY` macros in both projects' `ez80_hacks.md`: they
  synthesize indirect calls by stacking `$+6` before `jp (hl|ix|iy)`. The
  current AGNB callback trampoline deliberately retains its real-call plus
  tail-jump form because it avoids the macros' unconditional `BC` clobber.
- Replaced the hardware SD card's
  `/media/smith/AGON/mystuff/AgonWolf3D/tgt` with the reviewed local target.
  A recursive comparison passed byte-for-byte. The card contains
  `ui.agnb`, `font.agnb`, `images.agnb`, and the 29,120-byte application, with
  no loose `tgt/ui` or `tgt/fonts` directories. Physical runtime verification
  subsequently passed.

## Major milestone: all bitmap assets containerized

The user confirmed the final reviewed build runs successfully on physical
Agon hardware. Every bitmap family now loads through the generalized AGNB
image loader:

- UI and BJ weapon images from `ui.agnb`;
- proportional ITC Honda glyph images from `font.agnb`; and
- panels/cubes, sprites, and distance walls from `images.agnb`.

No loose bitmap payloads or generated per-image filesystem loaders remain in
the deployable target. The complete build, independent byte-level validation,
two-pass assembly, emulator test, exact SD-card deployment comparison, and
physical runtime test all passed. Sound effects remain intentionally loose
until the canonical AGNB project defines and proves its audio-container
records and loader behavior.

## AGNB 0.2 sound-effect integration

1. [x] Merge the canonical application-neutral `agnb_api.inc` into Wolf3D
   without losing its completed-image callback hook.
2. [x] Preserve the original `agnb_load_images` entry point with a no-op
   callback and add `agnb_load_images_with_callback` for Wolf3D's splash and
   breadcrumb behavior.
3. [x] Replace the harness-specific `0xB7E000` transfer window with
   loader-owned storage because Wolf3D uses that SRAM for map and sprite
   state.
4. [x] Convert the 22 source effects to unsigned 8-bit mono PCM build
   intermediates while preserving their 16,384 Hz sample rate.
5. [x] Generate `tgt/sfx.agnb` with explicit buffer IDs `0xFB00` through
   `0xFB15`.
6. [x] Remove the loose target audio payloads, filenames, and hard-coded
   per-file load routines while retaining playback IDs and duration metadata.
7. [x] Load and finalize the audio container during application startup using
   the AGNB 0.2 API and explicit sample rate.
8. [x] Independently validate all 22 container records byte for byte.
9. [x] Complete the full master build and final two-pass assembly.
10. [x] Retrofit the generic callback API and safe transfer storage into the
    canonical `/home/smith/Agon/mystuff/agon-utils` checkout, document the
    production-integration feedback there, and pass both canonical harness
    assemblies and all five focused tests.
11. [x] Confirm startup and representative sound-effect playback in the
    emulator.
12. [x] Deploy to physical hardware and confirm gameplay, sound playback, and
    program exit behavior.

The independently validated `tgt/sfx.agnb` contains 22 records, is 291,664
bytes, and has SHA-256
`0305da3914fed3ffd26555e72baab42fb7c57fd7682a26852f04a090fad5eaa9`.
The final merged application assembled to 28,518 bytes before runtime
verification.

The project `.venv` had stale interpreter symlinks after its previous Python
installation disappeared. They now select the installed stable Python 3.14.6
and continue to import the editable canonical `agonutils` package from
`/home/smith/Agon/mystuff/agon-utils`.

## Major-major milestone: all current image and audio assets containerized

The user confirmed the complete application starts, reaches gameplay, and
plays its newly containerized sound effects successfully in the emulator.
Wolf3D now loads UI images, proportional font glyphs, panels, sprites,
distance walls, and all 22 sound effects from AGNB containers. The full master
build, independent audio-container validation, final assembly, exact emulator
deployment comparison, and runtime test all passed.

This is the clean functional endpoint for the AGNB integration project.
Remaining work before an official release is cleanup and presentation rather
than another asset-loading conversion. Physical-hardware validation passed:
the application loaded, ran, played sound, and exited without a Guru
Meditation after a clean boot.

The previously observed Guru Meditation on exit now appears correlated with
AgonJukebox having run earlier in the same boot. AgonJukebox is the media
player maintained in the local AgonVideo project. This should be investigated
there as a likely failure to restore or release some VDP state or buffers,
rather than treated as a current Wolf3D exit defect.
