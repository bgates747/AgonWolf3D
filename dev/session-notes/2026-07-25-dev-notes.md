# Development Notes — 2026-07-25

## MapMaker emulator environment

- Confirmed that `dev/mapmaker` is a deprecated layout containing redundant
  application copies. Added a prominent `DEPRECATED.md` directing future work
  to `src/mapmaker`; retained the old tree pending a content audit.
- Added `src/mapmaker/setup_emulator.py`. It creates one ignored emulator
  profile at `src/.emulator/mapmaker`, outside the mapped working directory,
  and links `sdcard/mapmaker` directly to `src/mapmaker` without recursion.
- The profile uses non-ADL `bbcbasic.bin`. BBC BASIC starts correctly, and
  MapMaker runs when entered manually at its prompt with:

  ```basic
  LOAD"MAPMAKER.BAS"
  RUN
  ```

- Automatic MapMaker startup from `autoexec.txt` is not yet working. The
  attempted `RUN . MAPMAKER.BBC` line does not execute the program after
  `bbcbasic.bin` starts. Current MOS startup syntax remains to be confirmed;
  this does not block manual map editing.
- Edited the active `src/mapmaker/00_0.map` map using this environment.
